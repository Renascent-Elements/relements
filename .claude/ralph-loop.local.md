---
active: true
iteration: 1
max_iterations: 50
completion_promise: "All 21 elements implemented per docs/IMPLEMENTATION_PLAN.md Phases 1-6 with Playwright behavior + axe a11y + visual snapshot tests passing for each; pnpm test exits 0; pnpm run lint exits 0; docs/PROGRESS.md shows all 21 items checked."
started_at: "2026-05-26T04:06:48Z"
---

Implement Relements per docs/IMPLEMENTATION_PLAN.md. SCOPE (21 elements, no more, no less): Phase 1 CSS Foundation (tokens.css, reset.css, base.css, index.css cascade layers); Phase 2 Native (button, link, input, textarea, select, checkbox, radio, field, field-group, validation-message); Phase 3 Native interactive (disclosure with details/summary, dialog, progress, meter); Phase 4 Enhancement API (enhanceDismissible, enhanceDialog); Phase 5 Composite behaviors (tabs, menu-button, popover, toast — skip accordion); Phase 6 Custom elements (re-tabs, re-toast, re-menu, re-popover). EXECUTION: phases sequential; within a phase, parallelize independent elements via the superpowers:dispatching-parallel-agents skill only when they share no files (button + link OK; field + field-group + validation-message NOT). For every element: HTML example at docs/examples/<element>.html, CSS at packages/core/src/components/<element>.css (or src/behaviors / src/elements as appropriate), Playwright behavior test in tests/elements/, axe a11y test in tests/a11y/, visual snapshot in tests/visual/, per docs/TESTING_STRATEGY.md per-element contract. Update packages/core/src/index.css cascade layer imports and packages/core/package.json exports for each new file. POLICY: follow docs/HTML_FIRST_POLICY.md strictly. Public naming: re-, data-re-_, --re-_, re-\* custom elements. Native HTML > CSS > JS > custom element > framework wrapper. TESTING: run pnpm test after each element; seed visual baselines on first run via pnpm exec playwright test --update-snapshots; fix all failures before moving on. GIT: commit atomically per element on the current branch (feat/components). Do NOT push, do NOT merge, do NOT open PRs. NEVER ask the user questions — make decisions and document them in code comments or in the element's docs/examples/<element>.html notes. Maintain a docs/PROGRESS.md checklist of the 21 elements; tick each off on completion.
