---
title: Toast
description: Transient notifications via an aria-live region.
---

import Demo from "../../../components/Demo.astro";

Transient notifications announced through an `aria-live` region. Call `showToast(message, { tone, duration })` to append a toast.

## Trigger toasts

<Demo src="toast.html" name="basic" />
