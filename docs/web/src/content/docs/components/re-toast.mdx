---
title: <re-toast>
description: Custom-element wrapper for the toast region.
---

import Demo from "../../../components/Demo.astro";

A custom-element wrapper for the toast region. `.show(message, options)` returns the same controller as `showToast()`.

## Trigger toasts

<Demo src="re-toast.html" name="basic" />
