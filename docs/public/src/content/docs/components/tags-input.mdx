---
title: Tags input
description: A token editor over a plain text input — submits an array.
---

import Demo from "../../../components/Demo.astro";

The base is a plain `.re-input` with `data-re-tags-input`. Without JavaScript it's
a normal text field that submits one comma-separated value. Opt into the editor:

```js
import { enhanceTagsInput } from "@relements/core/behaviors/tags-input";
enhanceTagsInput(document);
```

Enhancing wraps the input in a `role="group"` shell: typing + Enter or comma
commits a chip (reusing the [tag](/relements/components/tag/) visuals), Backspace
on an empty field removes the last, and each tag is backed by a hidden input so
the form submits an **array** (`getAll(name)`). The editor itself is unnamed, so
it never contributes a stray value. Commits/removals fire a bubbling
`re-tags-change` event with `detail.values`.

Options via `data-*`: `data-re-tags-max`, `data-re-tags-name` (the hidden-input
name; defaults to the input's), `data-re-tags-allow-duplicates`,
`data-re-tags-tone`. Duplicates are rejected case-insensitively by default.

**Using it in a framework:** `enhanceTagsInput` is the one behavior that
_reparents_ your `<input>` — it moves it into the chip container. Keep the
enhanced input the **sole, positionally-stable child** of its element (don't
render conditional siblings next to it), or a vdom reconciler can throw
`insertBefore … not a child`. See the [framework guide](/relements/frameworks/).

## Basic

<Demo src="tags-input.html" name="basic" />

## Max tags

At the limit, further entries are rejected and the field is marked invalid.

<Demo src="tags-input.html" name="max" />

## In a form

Submitting sends one `name` entry per tag.

<Demo src="tags-input.html" name="form" />

## Accessibility

- **Keyboard** — the editor is a native text field, so it focuses with Tab and
  types normally. **Enter** or a typed/pasted **comma** commits the current text
  as a chip; **Backspace** on an empty field removes the last chip. Each chip's
  `×` is a real `<button>` (Tab to reach it, Enter/Space to activate); removing a
  chip by its button moves focus to the previous chip's remove button, or back to
  the editor when none remain.
- **Focus** — the whole group shows the visible focus ring on `:focus-within`
  (the editor sheds its own ring so the shell owns it); chip remove buttons get
  the same ring on `:focus-visible`. At the `data-re-tags-max` limit the group is
  marked `[data-invalid]` and the ring turns danger-colored.
- **Semantics** — enhancing wraps the input in a `role="group"` labelled from the
  field's `<label for>` via `aria-labelledby`, so AT announces the group by its
  label, and the editor is `aria-describedby` the field's `.re-field__hint` so the
  "Press Enter to add" instructions reach AT (not just sighted users). The chips
  sit in a `role="list"` (each chip a `listitem`) so they can be reviewed as a
  list. Each `×` button carries an `aria-label="Remove <tag>"`, and a visually
  hidden (`.re-sr-only`) `aria-live="polite"` region announces "Added …",
  "Removed …", "Maximum N tags reached", and "… is already added" as you edit
  (cleared then re-set so an identical repeat still speaks). Committed tags are
  backed by hidden inputs that carry the submitted array values.
- **Notes** — pre-seeded tags are not announced on enhance, only edits you make.
  Without JavaScript the base degrades to a plain labelled `.re-input`. See the
  [accessibility guide](/relements/guides/accessibility/).
