---
title: <re-popover>
description: Custom-element wrapper around the native popover attribute.
---

import Demo from "../../../components/Demo.astro";

A custom-element wrapper around the native `popover` attribute. It auto-adopts `popover="auto"` on connect; `.show()`, `.hide()`, and `.toggle()` map to the native API.

## Basic

<Demo src="re-popover.html" name="basic" />
