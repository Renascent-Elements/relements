# Link workspace packages for any matching range (so the framework example
# apps, which use "@relements/core": "*" to stay openable in StackBlitz/CodeSandbox,
# still consume the LOCAL core build in dev/CI/e2e — not the published one).
link-workspace-packages=true
